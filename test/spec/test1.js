import TodoModule from  '../../src/Todo';

describe('Todo module', () => {


    beforeEach(() => {
    });

    it('should exist', done => {
        expect(TodoModule).not.toBeFalsy();

        done();
    });

});
